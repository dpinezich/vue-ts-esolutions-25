// npm i --prefix . express
// npm i --prefix . --save-dev @types/express
// npm i --prefix . --save-dev ts-node

import express from "express";
const app = express();

app.get("/", function (req, res) {
    res.se
})

app.listen()
