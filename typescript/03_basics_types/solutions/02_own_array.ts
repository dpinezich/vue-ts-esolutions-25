const days: string[] = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'];

function printFavoriteDay(day: string): void {
    console.log(`my favorite day is ${day}!`);
}

printFavoriteDay(days[3]);
