let my_name: string = "David";
let my_number: number = 40;
let my_bool: boolean = true;

function getFavoriteName(): string {
    return "Bob";
}

const getFavoriteNumber = (num: number): number => {
    return num;
}

getFavoriteName();
getFavoriteNumber(my_number);
